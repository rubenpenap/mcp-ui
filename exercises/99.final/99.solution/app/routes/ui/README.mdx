# UI Components

This directory contains embeddable UI components that can be used with MCP-UI.

## Components

### Token Input (`token-input.tsx`)

A form for users to enter validation tokens received via email.

### Journal Viewer (`journal-viewer.tsx`)

A scrollable interface for viewing journal entries with expandable details, mood indicators, location tags, and weather information.
